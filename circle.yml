test:
  override:
    - php -l ./
    - pwd
    - chmod a+x deploy_stag.sh
    - ls -lah
    - ls -lah .ssh/
    - cat .ssh/authorized_keys
    - ssh root@188.226.204.18
deployment:
  staging:
    branch: master
    commands:
      - ./deploy_stag.sh
